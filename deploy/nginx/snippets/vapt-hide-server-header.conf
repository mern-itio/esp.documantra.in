# Strip Server banner from nginx and upstream (VAPT L24).
# Ubuntu/Debian: apt install libnginx-mod-http-headers-more-filter
# Then ensure /etc/nginx/nginx.conf loads the module (often auto-enabled).
more_clear_headers Server;
more_clear_headers X-Powered-By;
